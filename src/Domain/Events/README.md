# Events

Reserved for domain events (e.g. `ProductCreatedEvent`, `ProductDeletedEvent`) if/when the
application adopts domain event driven side effects (audit logging, notifications, etc.).
Not required by the current CRUD only scope, so this folder is intentionally empty for now.

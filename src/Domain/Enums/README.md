# Enums

Reserved for domain enumeration types (e.g. order/status enums) as the domain model grows.
The current `Product`/`Item` model defined by the assessment doesn't require any enums, so
this folder is intentionally empty for now.
